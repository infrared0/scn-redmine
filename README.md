# scn-redmine
Redmine container for Seattle Community Network

## Usage

Update `docker-compose.yml` and `rufus-imap.rb` with the correct passwords.

Deploy into a standard Docker engine with:

    sudo docker compose up -d

